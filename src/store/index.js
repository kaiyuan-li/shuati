import Vue from 'vue'
import Vuex from 'vuex'

Vue.use(Vuex)

const state = {
  code: '',
  result: ''
}

export default new Vuex.Store({
  state,
  mutations: {
  },
  actions: {
  },
  modules: {
  }
})
